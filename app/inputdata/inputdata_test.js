'use strict';

describe('myApp.inputdata module', function() {

  beforeEach(module('myApp.inputdata'));

  describe('inputdata controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var inputdataCtrl = $controller('InputController');
      expect(inputdataCtrl).toBeDefined();
    }));

  });
});